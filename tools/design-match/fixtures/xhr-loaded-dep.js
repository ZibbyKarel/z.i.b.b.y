// Loaded by a <script crossorigin="anonymous"> tag — the attribute the cdn
// cache rewrite preserves. Over http://127.0.0.1 the request is same-origin,
// so CORS mode costs nothing and the attribute needs no stripping.
window.__designMatchDepLoaded = true;
