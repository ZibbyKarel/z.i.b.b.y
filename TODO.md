- [ ] Fix --agents catalog schema break: claude-tools.ts toSubagentTools() must return string[] instead of a comma-joined string, and CatalogEntry.tools in claude-run-command.service.ts must be readonly string[] instead of string — installed claude CLI 2.1.251 rejects the joined-string form for every catalog entry ('Invalid input: expected array, received string'), breaking every Forge pipeline stage that builds an --agents catalog (confirmed on delivery_1788181538481 and patch_1788181556628, both failed instantly with 0% progress)
- [ ] BUG: verdikt code-reviewera se v delivery pipeline nikdy nečte — fáze review nemá qualify: true (.zibby/data/pipelines/delivery.pipeline.md:18-34), takže pass/fail je jen exit code claude procesu (pipeline-runner.service.ts:1009-1024). Reviewer může napsat 'tohle je rozbité' a pipeline pokračuje dál na PR. Zapnout qualify na review i n-9
- [ ] BUG: goal 'claude' verifier ignoruje vlastní verdikt — prompt si vyžádá PASS/FAIL, ale výsledek je satisfied: status === 'done' (goal-runner.service.ts:650-670), tj. spokojený, když soudce nespadl. Doparsovat verdikt z logu, fail-closed
- [ ] Delivery pipeline nemá žádnou nezávislou deterministickou verifikaci — Kodér si kontroly (lint/typecheck/testy) spouští a reportuje sám. Mechanismus type: verify existuje (verify-command.ts, používá ho quick-fix/patch), ale v delivery chybí; navíc běží ve stejném worktree, ne z čisté větve. Přidat verify fázi s captured exit code jako evidence
- [ ] Chybí kritik, který produkt reálně proklikne, a skeptik pro research — qa-expert je zapojený jen v read-only quality-scan, ui-ux-tester (má Playwright) není v žádné pipeline, research.pipeline.md nemá refutační fázi. Doplnit QA fázi, která zkouší user journeys a edge cases a zakládá z nálezů nové úkoly
- [ ] Uvnitř jednoho běhu neexistuje paralelizace — jeden claude -p na run, fáze pipeline striktně sériově; souběžnost je jen mezi samostatnými top-level runy (cap maxConcurrentRuns: 3). Zvážit fan-out nezávislých úkolů jedné zakázky do vlastních git worktree (Albert: DAG scheduler + worktree per task + sériový merge v topo pořadí)
- [ ] Chybí plánovač, který z cíle vyrobí task list s file_scope a model tierem per úkol — pipeliny jsou ručně psané, model je statický (escalation ladder je pevná tabulka). Roadmap decomposition sice dělá dependsOn položky, ale bez file scopes, bez per-task volby modelu a bez re-planu za běhu. Dopsat plánovací fázi (levný tier na rutinu, drahý jen na těžké uvažování)
- [ ] Do běžícího runu se nedá promluvit — stdin je ignore (runner-core.ts:397), operátor má jen stop/resume/delete a poznámku až po zaparkování. Přidat inbox (soubory v sandboxu), který runner načte na začátku každé fáze: umožní přeprioritizovat nebo zastavit, ale nikdy neoslabit gate ani zvrátit zamítnutí revize
- [ ] rozdělit v chatu mentions (momentálně mention funguje jen přes "@") nově ale bude
  - "@" označovat agenty
  - "/" pouštět specifické skills zibbyho
  - "#" hledat firmy a týmy
- [ ] prozkoumat možnost pouštět nativní skills jako superpowers přes zibbyho resp aby si je zibby pouštěl sám. Je to vůbec dobrý nápad ? nebo skilly forknout a udělat z nich lokální zibby's skills ?
