# Dependencies
node_modules/

# Build outputs
dist/
.next/
out/

# Local agent storage (apps/api data dirs: live/dev + test, selected by ZIBBY_DATA_DIR)
apps/api/data-test/
apps/api/data/**/runs
apps/api/data/approvals
apps/api/data/tasks
apps/api/data/vault/daily
apps/api/data/credentials
apps/api/data/channels

!apps/api/data/agents
!apps/api/data/automations
!apps/api/data/skills


# Env / secrets — never commit
.env
.env.local
.env.*.local

# Local Claude Code session state (permissions granted interactively, MCP auth tokens)
.claude/settings.local.json

# OS
.DS_Store
Thumbs.db

# Editor
.idea/
*.swp

# Local-only Claude Design handoff bundle (not committed)
design-ref/

# Playwright e2e
/.e2e-data/
/playwright-report/
/test-results/
/e2e/.auth/

z-i-b-b-y
# Playwright MCP session artifacts (snapshots/console logs)
/.playwright-mcp/
