# Dependencies
node_modules/

# Build outputs
dist/
.next/
out/
*.tsbuildinfo

# graphify knowledge-graph output (regenerated via `graphify update .`, not versioned)
graphify-out/

# Local agent storage (apps/api data dirs: live/dev + test, selected by ZIBBY_DATA_DIR)
apps/api/data-test/
apps/api/data/**/runs
apps/api/data/approvals
apps/api/data/tasks
apps/api/data/vault/daily
apps/api/data/credentials
apps/api/data/channels
apps/api/data/integration-state
apps/api/data/activity
apps/api/data/budget-ledger
apps/api/data/research-digest.json
apps/api/data/mcp-credentials
apps/api/data/project-secrets
apps/api/data/_purged

!apps/api/data/agents
!apps/api/data/automations
!apps/api/data/skills
!apps/api/data/hooks
!apps/api/data/commands
!apps/api/data/mcp-servers


# Env / secrets — never commit
.env
.env.local
.env.*.local

# Local Claude Code session state (permissions granted interactively, MCP auth tokens)
.claude/settings.local.json

# OS
.DS_Store
Thumbs.db

# Editor
.idea/
*.swp

# Local-only Claude Design handoff bundle (not committed)
design-ref/

# Playwright e2e
/.e2e-data/
/playwright-report/
/test-results/
/e2e/.auth/

z-i-b-b-y
# Playwright MCP session artifacts (snapshots/console logs)
/.playwright-mcp/

# Stray fake-claude run artifacts (the e2e CLAUDE_BIN stub writes a marker + intent
# files into the api server's cwd when a gated agent run isn't sandboxed to a worktree)
/apps/api/agent-007-was-here.txt
/apps/api/intent-request.json
/apps/api/payment-done.txt
