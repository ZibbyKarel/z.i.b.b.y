# Dependencies
node_modules/

# Build outputs
dist/
.next/
out/
*.tsbuildinfo

# Electron desktop packaging: staged production artifacts + build output
apps/api/.stage/
apps/desktop/resources/
apps/desktop/release/
apps/desktop/dist/

# graphify knowledge-graph output (regenerated via `graphify update .`, not versioned)
graphify-out/

# superpowers brainstorming visual-companion session files
.superpowers/

# Local agent storage (apps/api data dirs: live/dev + test, selected by ZIBBY_DATA_DIR)
.zibby/data-test/
.zibby/data/**/runs
.zibby/data/approvals
.zibby/data/tasks
.zibby/data/vault/daily
.zibby/data/import
.zibby/data/credentials
.zibby/data/channels
.zibby/data/integration-state
.zibby/data/activity
.zibby/data/budget-ledger
.zibby/data/subsystem-seen.json
.zibby/data/artifacts
# Handoff: rules.json reseeds from the in-code SYSTEM_HANDOFF_RULES (code is the
# source of truth); proposals/ + fired/ are runtime. All regenerable, none versioned.
.zibby/data/handoff
.zibby/data/research-digest.json
.zibby/data/mcp-credentials
.zibby/data/project-secrets
.zibby/data/_purged
.zibby/data/chat

# Phase 76: per-machine clone-root config — never synced, differs per machine.
.zibby/data/machine/config.json

# Phase 73: uploaded avatar image bytes live outside the (committed) .md
# frontmatter — binary, regenerable from an upload, never versioned.
.zibby/data/agents/assets/
.zibby/data/pipelines/assets/
.zibby/data/projects/assets/

.!zibby/data/agents
.!zibby/data/automations
.!zibby/data/skills
.!zibby/data/hooks
.!zibby/data/commands
.!zibby/data/mcp-servers


# Env / secrets — never commit
.env
.env.local
.env.*.local

# Local Claude Code session state (permissions granted interactively, MCP auth tokens)
.claude/settings.local.json

# OS
.DS_Store
Thumbs.db

# Editor
.idea/
*.swp

# Local-only Claude Design handoff bundle (not committed)
design-ref/

# Playwright e2e
/.e2e-data/
/playwright-report/
/test-results/
/e2e/.auth/

z-i-b-b-y
# Playwright MCP session artifacts (snapshots/console logs)
/.playwright-mcp/

# Stray fake-claude run artifacts (the e2e CLAUDE_BIN stub writes a marker + intent
# files into the api server's cwd when a gated agent run isn't sandboxed to a worktree)
/apps/api/agent-007-was-here.txt
/apps/api/intent-request.json
/apps/api/payment-done.txt

# graphify: only GRAPH_REPORT.md (+ cost.json) is committed — the rest is
# machine-specific (interpreter path, absolute-path manifest) or regenerable.
/graphify-out/graph.json
/graphify-out/manifest.json
/graphify-out/cache/
/graphify-out/.graphify_python
/graphify-out/.graphify_*.json
/graphify-out/.graphify_*.txt

# live runtime data root (resolveDataRoot() default anchor — real operator data,
# regenerated locally e.g. by `pnpm self-knowledge:generate`; never versioned)
/apps/api/data/
/scratchpad/
